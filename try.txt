#TIMEHORIZON T = 3;

#NODE A

    #PARAMETERS
        b = 20;

    #VARIABLES
        internal auxiliary : aux[T];
        internal state : x[T];
        internal action : a[T];
        internal sizing : init;

    #CONSTRAINTS
        0 <= aux[t+1]*x[0];
        x[t+1] == a[t];
        init == 3;


    #OBJECTIVES
        min: x[t];