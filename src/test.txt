// working example where x = 8
#NODE Parent
    #PARAMETERS
        a(i, j) <- 2 * i + j;
        b = {a, a(9, a(3, 4))};
        c(arr, i) <- arr[i-1];
        c2(dict, key) <- dict[key];
        d(fct, val) <- fct(val*2);
        e(i) <- i;
        k = b[0](i for i in [1:2]) - c(b, 1)(1,2);  // k = 0
        m = c({{2}}, 1)[0] - d(e, 1) + n;  // m = 0
        n = {"0": e}["0"](0);  // n = 0
        
        z = -((import "test.csv"))[0](1);
        y = @test + 3;
        t = {{tt} for tt in [1:2]};
        //yy = ("a") + 1;
        w = {"test"};
        w1 = c2({"0":0}, "0");
        ww = 2. + {1};
        zz = z[{1}];
        zz = b | {wa*2 for wa in [1:3:2]};
        aaa = -(1)**{1};
        
        //k = a(0);
        //b(a) <- 2*2;
        a1 = 1(5);  // TODO should not be accepted by Lark
    #NODE Child
        #VARIABLES
            internal : x;
        #CONSTRAINTS
            parent.a(z+3,2) >= x + Parent.k + Parent.m for z in [0:2];
        #OBJECTIVES
            max : x;
    #VARIABLES
        pass;
